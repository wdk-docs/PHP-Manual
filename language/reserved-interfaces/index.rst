预定义接口
=================================

class.traversable 遍历 — Traversable（遍历）接口
class.iterator 迭代器 — Iterator（迭代器）接口
class.iteratoraggregate 聚合式迭代器 — IteratorAggregate（聚合式迭代器）接口
class.arrayaccess 数组式访问 — ArrayAccess（数组式访问）接口
class.serializable 序列化 — 序列化接口
class.closure Closure — The Closure class
